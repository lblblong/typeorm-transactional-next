import { createNamespace } from 'cls-hooked'

export const NAMESPACE_KEY = '_lbl_transaction_'
export const ENTITY_MANAGER_KEY = '_lbl_transtion_entity_manager_'

export const ns = createNamespace(NAMESPACE_KEY)
